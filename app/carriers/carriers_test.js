'use strict';

describe('myApp.carriers module', function() {

  beforeEach(module('myApp.carriers'));

  describe('carriers controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var carriersCtrl = $controller('CarriersCtrl');
      expect(carriersCtrl).toBeDefined();
    }));
  });
});