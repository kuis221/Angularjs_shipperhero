'use strict';

describe('shipperApp.rfp module', function() {

  beforeEach(module('shipperApp.rfp'));

  describe('rfp controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var rfpCtrl = $controller('RfpCtrl');
      expect(rfpCtrl).toBeDefined();
    }));
  });
});